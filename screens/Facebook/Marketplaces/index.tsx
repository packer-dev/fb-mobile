import * as React from "react";
import Container from "../Container";

const Marketplaces = ({ route }: any) => {
  return <Container scrollView route={route}></Container>;
};

export default Marketplaces;
